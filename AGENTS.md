# AI Knowledge Structuring Agent — 开发规范

本文件是本仓库所有开发、代码评审和 AI 协作的长期约束。若与临时实现便利性冲突，以本文件为准；架构层面的变更须先更新本文件与 README。

## 1. 项目目标

构建一个 AI 驱动的文档理解与知识探索系统：用户上传 PDF、DOCX、PPTX 等资料后，系统将其解析为带来源定位的内容块，生成可编辑知识结构，并提供带证据引用的节点问答。

核心闭环：`上传文件 → 文档处理 → 知识结构 → 证据核验 → 节点问答`。

非目标：本项目不是简单摘要工具；任何无来源、不可验证的模型结论都不应作为产品结果展示。

## 2. 架构边界

- `frontend/`：Next.js + TypeScript + Tailwind，仅负责用户界面与 BFF 边界内的轻量调用。
- `backend/`：FastAPI + Python，负责 API、鉴权、业务编排、任务状态和持久化边界。
- `backend/app/agents/`：LangGraph 工作流，只负责受控的 AI 推理与结构化输出。
- `backend/app/workers/`：异步文档处理任务；不得在 HTTP 请求中执行长耗时解析或模型调用。
- PostgreSQL：事务数据、知识节点、关系、证据、任务元数据。
- ChromaDB：内容块向量与语义检索；不能成为唯一事实来源。
- MinIO/S3：原始上传文件和衍生文件。

不得绕过 API 直接从前端访问数据库、ChromaDB 或内部 Worker。

## 3. 编码规范

### 通用

- 所有源代码、配置键、API 字段和提交信息使用英文；面向用户的文案可使用中文。
- 优先小而清晰的模块，单个文件应围绕一个职责。
- 不提交密钥、令牌、真实用户文件、模型输出缓存或构建产物。
- 新增依赖必须说明用途，并锁定在对应依赖清单中。

### Frontend

- 使用 TypeScript 严格模式；禁止 `any`，确有必要时使用 `unknown` 并收窄类型。
- 组件采用 PascalCase；hooks 以 `use` 开头；API 客户端放入 `src/lib/api/`。
- 页面只负责组合；可复用 UI 放入 `src/components/`，领域状态放入 `src/features/`。
- 使用 Tailwind 工具类，避免无约束的全局 CSS。

### Backend

- Python 使用类型注解；函数和变量使用 `snake_case`，类使用 `PascalCase`。
- API 路由保持薄层：校验 → 调用 service → 返回 schema。
- 使用 Pydantic 定义所有请求/响应边界；数据库模型不得直接作为 API 响应。
- 业务逻辑放入 `services/`，数据库访问放入 `repositories/`，避免循环依赖。
- 所有错误都应有可诊断日志与面向客户端的安全错误响应。

## 4. 文件组织规则

- 仅在所属领域目录内新建文件；不要把业务代码堆入 `main.py`、页面文件或路由文件。
- 文档解析器必须实现统一的内容块输出契约，保留 `document_id`、页/幻灯片位置与文本定位信息。
- 知识节点、边和回答必须通过证据实体关联至内容块。
- 数据库 schema 变更必须配套迁移、测试和 README/架构文档更新。
- 环境变量只能从 `.env` 读取；`.env.example` 必须保留无敏感值的完整模板。

## 5. Git 规范

- 使用 Conventional Commits：`feat:`、`fix:`、`docs:`、`refactor:`、`test:`、`chore:`。
- 一个提交只解决一个逻辑问题；不混入格式化、重构和功能变更。
- 不直接提交到受保护主分支；使用短生命周期功能分支，例如 `feat/document-upload`。
- 合并前必须通过 lint、类型检查和相关测试；PR 描述需说明范围、验证方式和架构影响。
- 不使用破坏性 Git 操作覆盖他人改动。

## 6. 测试要求

- Frontend：Vitest/Testing Library 覆盖关键组件与 API 状态；Playwright 覆盖核心用户链路。
- Backend：pytest 覆盖 service、repository、API 边界和失败路径。
- Agent：每个图工作流须有固定输入、结构化输出校验、证据绑定校验与拒答测试。
- 文档处理：维护脱敏样本文档集，测试解析成功率、来源位置完整性和重试行为。
- 新功能至少包含正常路径与一个错误/边界路径测试。

## 7. AI Agent 开发原则

- Evidence first：先检索/提取证据，再生成结论；禁止凭空补全事实。
- Structured output：节点、关系、计划和工具结果必须经过 schema 校验。
- Scope control：每次 Agent 执行必须有明确工作区、文档或节点范围。
- Traceability：记录模型、提示版本、输入范围、检索证据、耗时与成本。
- Fail safely：证据不足、解析失败或 schema 无效时拒答/降级，不伪造成功结果。
- Human correction：用户对节点、关系和结论的修订优先于后续自动重建。
- LangGraph 节点必须职责单一、可重试、可观测；禁止不可控的无限循环。

## 8. 架构变更控制

- 不允许随意修改既定架构、替换基础设施或跨层调用。
- 以下变更须先形成 ADR 或在 PR 中获得明确批准：替换数据库/向量库/队列/模型供应商；新增微服务；改变证据数据模型；移除异步流水线；改变鉴权边界。
- 任何架构变更必须说明：问题、替代方案、迁移计划、兼容性、回滚方案、成本和观测影响。
- MVP 阶段优先在既定单体仓库内演进；只有在性能、可靠性或团队边界有实测证据时才拆分服务。
