# Infrastructure

Docker Compose at the repository root runs the local development services. Keep production deployment manifests and infrastructure-as-code in this directory; do not embed production credentials in Compose files.
